# Template only — copy this file to ".env" and fill in your REAL values there.
# Never put real secrets in THIS file (it is committed to git).
# The real .env is gitignored and stays on your machine only.

# From @BotFather when you create the bot:
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ

# Your group's chat id (a negative number). Find it with: python get_chat_id.py
TELEGRAM_CHAT_ID=-1001234567890
